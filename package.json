{
  "private": true,
  "scripts": {
    "webdriver-update": "webdriver-manager update --gecko true --chrome false --standalone false",
    "install": "npm run webdriver-update || npm run webdriver-update",
    "test": "percy exec -- protractor conf.js"
  },
  "devDependencies": {
    "@percy/cli": "^1.0.0-beta.67",
    "@percy/protractor": "^2.0.0",
    "http-server": "^13.0.1",
    "protractor": "^7.0.0",
    "todomvc-app-css": "^2.4.1"
  }
}
